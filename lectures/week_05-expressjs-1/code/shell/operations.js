function factorial(n) {
  // complete during class
}

function square(n) {
  // complete during class
}

function sqroot(n) {
  // complete during class
}

module.exports = {
    factorial: factorial,
    square: square,
    sqroot: sqroot
};
